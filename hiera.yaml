---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "nodes/base.yaml"
      - "bla.yaml"
      - "common.yaml"
      - "test.yaml"
      - "test2.yaml"
